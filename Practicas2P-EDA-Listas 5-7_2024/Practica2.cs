using System;
using System.Collections.Generic;
using System.Linq;
/*
namespace Forms
{
    public class Práctica2
    {
        private List<int> numeros; // Lista para almacenar números generados
        private Random random;

        public Práctica2()
        {
            numeros = new List<int>();
            random = new Random();
        }

        // Genera 20 números aleatorios y los guarda en la lista
        public void GenerarNumeros()
        {
            numeros.Clear(); // Limpia lista anterior
            for (int i = 0; i < 20; i++)
                numeros.Add(random.Next(1, 100)); // Agrega números nuevos
        }

        // Devuelve los números pares de la lista
        public List<int> ObtenerPares()
        {
            return numeros.Where(n => n % 2 == 0).ToList();
        }

        // Devuelve los números impares de la lista
        public List<int> ObtenerImpares()
        {
            return numeros.Where(n => n % 2 != 0).ToList();
        }
    }
}
*/