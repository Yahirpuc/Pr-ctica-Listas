using System;
using System.Collections.Generic;
using System.Linq;
/*
namespace Forms
{
    public class Práctica3
    {
        private List<Alumno> alumnos; // Lista para almacenar alumnos

        public Práctica3()
        {
            alumnos = new List<Alumno>();
        }

        // Agrega un alumno con su nombre y calificación
        public void AgregarAlumno(string nombre, int calificacion)
        {
            if (!string.IsNullOrEmpty(nombre))
                alumnos.Add(new Alumno(nombre, calificacion));
        }

        // Devuelve la lista de alumnos aprobados (>=7)
        public List<Alumno> ObtenerAprobados()
        {
            return alumnos.Where(a => a.Calificacion >= 7).ToList();
        }

        // Devuelve la lista de alumnos reprobados (<7)
        public List<Alumno> ObtenerReprobados()
        {
            return alumnos.Where(a => a.Calificacion < 7).ToList();
        }
    }
}
*/